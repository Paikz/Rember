<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.remberapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ContactsActivity" />
        <activity android:name=".AddReminderActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Register2Activity" />
        <activity android:name=".Options.OptionGeneralActivity" />
        <activity android:name=".Options.OptionThemeActivity" />
        <activity android:name=".Options.OptionNotificationActivity" />
        <activity android:name=".Options.OptionChangeNumberActivity" />
        <activity android:name=".Options.OptionAboutActivity"></activity>
    </application>

</manifest>